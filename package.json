{
  "name": "tictactoe",
  "version": "1.0.0",
  "description": "test ttt multiplayer",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm start --prefix client"
  },
  "author": "Ludo Corsini",
  "license": "MIT"
}
